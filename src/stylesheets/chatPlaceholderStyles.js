import { StyleSheet } from "react-native";

export default StyleSheet.create({
  background: {
    flex: 1,
    // flexWrap: "wrap",
    alignItems: "center",
    // alignContent: "center",
  },
  chosenCharacter: {
    height: "40%",
    resizeMode: "contain",
  },
});
