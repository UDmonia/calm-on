// checkout the current selections
const checkout = ({outFitList, total}) => (dispatch) => {
  // modifies current bought items
};

// get current coins
const getCoins = (dispatch) => {
};

// get currently equiped
const getEquiped = (dispatch) => {

};

// replace the current equiped list of items
const equipedSnapshot = ({outfitList}) => (dispatch) => {

};
